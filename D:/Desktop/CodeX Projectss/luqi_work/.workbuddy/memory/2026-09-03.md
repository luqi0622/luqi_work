# 2026-09-03 工作记录

## 把 QQ空间说说后端合并进 luqi.work（单项目）

- **决定**：用户确认把随笔（说说）后端做成 luqi.work 的一个整体，不再用 iframe 跨项目嵌入旧的 `qzone-shuoshuo`。
- **完成内容**：
  - `/blog` 改为原生服务端渲染页（`prerender=false`），直接读 Turso 渲染说说；顶部标签筛选、游客评论+楼中楼（角色「游客」）、博主密码登录弹窗（Header）、写新随笔框、每条说说三点菜单（编辑/置顶/标签/删除）、博主回复+删评论（「博主」徽章）。
  - 后端迁入 `src/lib/{db,auth,queries}.ts` + `src/pages/api/*`（login/logout/me、shuoshuo/[id]/comments、admin/posts、admin/comments、admin/tags）。
  - 评论存 `posts.comments` JSON 树：`{id,name,time,content,parentId,role,replyTo}`；旧评论由 `rowToPost()` 自动补全，无需迁移脚本；删评论用 `collectSubtree` 连带删子树。
  - `astro.config.mjs` 改 `output:'static'`（Astro 5.18 已移除 'hybrid'）+ `@astrojs/vercel`；`vercel.json` 加 buildCommand。
  - `middleware.ts` 保护 `/api/admin/*`（未登录 401），并加 `/api/me` 探测登录态。
- **关键坑（已解决）**：`npm run build` 在 Windows 卡死 ~18min —— nft 追踪 `@libsql/client` 原生模块 `@libsql/win32-x64-msvc` 卡住（路径含空格）。改用 `@libsql/client/web`（纯 JS，Turso https 本就走 HTTP，不需要原生模块）后构建 ~3min 正常完成。副作用：本地 `file:` SQLite 不可用，本地调试需填真实 `TURSO_*`。
- **构建注意**：本机需 `CODEBUDDY_SAFE_DELETE_ENABLED=0` 让 Vercel 适配器清理 `.vercel/output`；用托管 Node `22.22.2-2/npm.cmd` + npmmirror 源。
- **部署 env（Vercel `luqi-work`）**：`TURSO_DATABASE_URL`、`TURSO_AUTH_TOKEN`（运行时）、`ADMIN_PASSWORD_HASH`（运行时，login.ts 用 process.env）、`SESSION_SECRET`（**构建期** import.meta.env，改它必须全新构建）。本地 `.env` 已含后两个（gitignore）。
- 更新了 `agent.md` §0/§2/§6/§7/§10 反映合并架构与运维 SOP。

## 待办 / 未验证
- 未做真实运行预览（缺 Turso 凭据）；仅通过 `npm run build` 全绿验证编译。上线前建议本地 `npm run dev` + 真实 `TURSO_*` 跑一遍登录/评论/楼中楼。

## 部署引导（进行中，未上线）
- 我无法直接写 Vercel env（无连接连接器）。已给完整 4 变量清单 + 小白 push/部署步骤（A 填 env → B `git push origin main` → C 等 Ready → D 验 `/blog`）。
- 用户在 A-0 卡住：Vercel 把 env 值遮成 `••••`、复制键灰。已给两条解法：① 控制台点 👁 显形再复制（换 Chrome/Edge）；② 直接去 turso.tech 拿 `TURSO_*`（最稳，新令牌同库等效）。已把这条沉淀进 `agent.md` §6.1。
- 现状：本地已 `git commit`（c0a5016，27 文件 +3532/−105），**尚未 push、未部署**；部署待用户在 Vercel 填 4 个 env 后 push。
- 已在 `agent.md` §0/§2/§6/§7/§10 反映合并架构、libsql/web 构建修复、运维 SOP。
