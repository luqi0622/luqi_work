# 2026-08-31 · luqi.work 博客迭代

## 上线进展
- 用户已完成 GitHub 推送 + Vercel 导入，项目名 `luqi-work`
- 阿里云 DNS 两条记录（A @ → 76.76.21.21；CNAME www → cname.vercel-dns.com）配置正确，luqi.work 已能正常访问新博客
- 环境变量 PUSH_PLUS_TOKEN 暂不配置（用户决定先不启用田原食品留言微信推送；代码已做空值兜底，不配不影响部署）

## 本次改动（三件事）
1. **QQ 空间说说全量导入**
   - 新建 `scripts/import-qq-shuoshuo.mjs`：解析根目录 `QQ空间说说备份.md`，生成 223 篇 Markdown 到 `src/content/blog/`
   - 处理：清理 `[em]` 表情码、`#话题#` 转粗体、同名分钟冲突加序号、纯图片无评论条目跳过
   - 初步分类（关键词打分规则）：心情随笔 132 / 校园青春 25 / 亲情爱情 22 / 读书观影 14 / 梦想奋斗 13 / 生活日常 17
   - 历史评论以「当时朋友们的评论」章节附在正文后
   - 原来 4 篇示例文章已删除
2. **青春化风格改造**
   - 字体：`@fontsource/zcool-kuaile`（站酷快乐体，标题/品牌）+ `@fontsource/noto-sans-sc`（正文），global.css 定义 `--font-display` → `font-display` 工具类
   - 配色：indigo 系全部换成 rose/fuchsia/violet 糖果渐变（`.text-gradient`、`.chip-active`、卡片 hover 渐变条）
   - 新增 `src/utils/tags.ts`：6 个分类各自的糖果色 chip 配色（violet/rose/amber/sky/emerald/teal）
   - 圆角加大（rounded-2xl/3xl）、hover 位移放大、Hero 背景漂浮气泡（`animate-float-slow`）
3. **首页 Hero 右侧动漫半身像位**
   - `src/config.ts` 新增 `HERO_IMAGE = '/images/hero-anime.svg'`
   - `public/images/hero-anime.svg` 为自制占位图（480×600，含提示文字）；用户之后放同名 png 改配置路径即可
   - Hero 改为 `md:grid-cols-[minmax(0,1fr)_auto]` 左右布局

## 构建状态
- `npm run build` 全绿：228 个页面，dist 27MB（主要是 Noto Sans SC 三档字重的 unicode-range 子集，浏览器按需加载）
- 本地预览 http://127.0.0.1:4321 全部 200

## 待办
- 用户填 `src/config.ts` 里的 GitHub/邮箱/Scholar 链接
- 启用 giscus 评论（填 repo/repoId/categoryId）

## 追加改动（同日晚）
4. **Hero 动漫图接入**：用户放了 `public/images/动漫相片2.jpg`（765×1024 JPEG，格式没问题），
   因是中文文件名+config 未指向它而不显示。已复制为 `hero-anime.jpg` 并将
   `src/config.ts` 的 `HERO_IMAGE` 改为 `/images/hero-anime.jpg`。
   → 经验：public 下的资源用英文文件名，避免中文路径编码问题
5. **分类可手动修正**：新增 `src/data/tagOverrides.ts`（`TAG_OVERRIDES: Record<string, string[]>`），
   `src/utils/tags.ts` 加 `resolveTags(postId, frontmatterTags)`（覆盖优先），
   PostCard / [slug].astro 均已接入
6. **写作工具箱页面 `/tools`**（页脚有入口）：
   - 新建随笔表单 → 生成 Markdown 预览 / 下载 .md / 复制（文件名 `notes-YYYYMMDD-HHMM.md`）
   - 223 条随笔分类一览表 + 搜索 + 下拉改分类 → 导出 `tagOverrides.ts`
   - 纯前端，导出文件需用户手动放回项目再推送才生效
7. **构建脚本修复**：`scripts/build-tianyuan.mjs` 新增 `removeDirRobust()`。
   环境里 Node 的 `fs.rmSync` 被安全删除机制接管（走回收站），大目录会报
   "Some operations were aborted"，导致 vite 的 emptyDir 和产物同步都失败。
   解法：构建前先自己清空 `TianYuanFood/dist` 与 `public/tianyuanFood`，
   降级顺序 fs.rmSync → 系统命令 rmdir/rm -rf → 逐文件删除 → cpSync force 覆盖

## 构建状态（最新）
- `npm run build` 全绿：229 页（含 /tools），dist 约 27MB（Noto Sans SC 子集字体为主）
